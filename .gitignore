# secrets — never commit
x_secrets.txt
.env
# مفتاح حساب خدمة Firebase: يمنح إرسال إشعارات لكل أجهزة المشروع،
# فيبقى على الخادم وحده ولا يدخل المستودع.
*service-account*.json
*firebase-adminsdk*.json
fcm_key*.json
*.keystore
*.jks
key.properties

# flutter
app/.dart_tool/
app/build/
app/.flutter-plugins*
app/.packages
app/pubspec.lock
app/.idea/
*.iml

# worker
worker/node_modules/
worker/.wrangler/
worker/.dev.vars

# misc
flutter_sdk.zip
nul
worker/owner_bootstrap.py

app/android/build/
app/android/local.properties
app/android/app/.cxx/
app/android/.gradle/
dist/

# نسخ قاعدة البيانات الحيّة: بيانات مستخدمين حقيقية — لا تُرفع أبداً
backups/
