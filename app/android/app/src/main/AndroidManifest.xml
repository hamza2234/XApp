<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- مطلوب لكل اتصال شبكي — التطبيق يعمل عبر Worker خارجي فقط -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- إشعارات إعلانات المالك — أندرويد 13+ يطلبها صراحةً.
         POST_NOTIFICATIONS غير معلَنة تعني ألا يظهر أي إشعار أبداً. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- اهتزاز الإشعار: أندرويد 13+ يحتاج VIBRATE صراحةً مع POST_NOTIFICATIONS -->
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- تسجيل الرسائل الصوتية في الدردشة — يُطلب وقت التسجيل لا عند التثبيت -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <application
        android:label="MAPX"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- قناة رسائل FCM الافتراضية: رسالة نظام لا تُوسم بقناة تظهر على
             قناة الإفلات الافتراضية بأهمية منخفضة، فلا توقظ الشاشة. تسميتها
             هنا يضمن أن إشعاراً وصل قبل بناء قنواتنا لا يضيع بصمت. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="mapx_chat" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- أندرويد 11+ يخفي التطبيقات غير المعلَنة، فبدون هذه الاستعلامات
             يعيد canLaunchUrl القيمة false ولا يُفتح تيليجرام -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="http"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tg"/>
        </intent>
        <package android:name="org.telegram.messenger"/>
        <package android:name="org.telegram.messenger.web"/>
        <package android:name="org.telegram.plus"/>
        <package android:name="com.telegram.messenger"/>
    </queries>
</manifest>
